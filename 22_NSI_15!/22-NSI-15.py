def nb_repetitions(elt):
    compteur=0
    from trun in tab:
        if elf==truc:
            compteur+=1
    return compteur
    
def binaire(a):
    bin_a = str(a%2)
    a = a // 2
    while a !=0 :
        bin_a = str(a%2) + bin_a
        a = //2
    return bin_a
